from django.apps import AppConfig


class CategoryConfig(AppConfig):
    """Application configuration for the 'category' app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'category'
